# 自定义卡牌效果卡牌对决游戏 - 技术设计文档

## 1. 系统架构概述

### 1.1 核心分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    表现层 (Presentation)                 │
│    网页 (HTML/CSS/JS) → 动画、特效、SVG箭头拖拽交互      │
├─────────────────────────────────────────────────────────┤
│                    客户端 (Client)                       │
│    状态同步 → 输入处理 → 拖拽交互 → 目标锁定             │
├─────────────────────────────────────────────────────────┤
│                    游戏核心 (Game Core)                  │
│    规则引擎 → 效果系统 → 状态管理 → 事件系统 → 角色牌    │
├─────────────────────────────────────────────────────────┤
│                    数据层 (Data)                         │
│    卡牌定义 → 角色牌定义 → 规则配置                      │
└─────────────────────────────────────────────────────────┘
```

### 1.2 推荐技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 规则引擎 | 原生 JavaScript | 数据驱动的确定性引擎 |
| 客户端 | 原生 HTML/CSS/JS | 网页版，双击即可运行 |
| 状态管理 | 不可变状态对象 | 支持拖拽交互 |
| 卡牌定义 | 常量对象 + 配置表 | 数据驱动配置 |
| 角色牌 | CharacterDefinitions 对象 | 技能+装备效果 |

---

## 2. 核心游戏规则

### 2.1 基础规则

| 规则 | 数值 | 说明 |
|------|------|------|
| 血量上限 | 10点 | 玩家初始血量（角色牌可修改） |
| 炸血 | 血量≥11 | 血量超过上限时触发，血量归0 |
| 濒死状态 | 血量=0 | 触发濒死状态 |
| 濒死回合 | 2回合 | 连续2回合处于濒死状态则死亡 |
| 游戏倍率 | 初始1 | 通过"发怒"卡牌累加 |

### 2.2 游戏状态结构

```typescript
interface GameState {
  players: Player[];           // 玩家列表
  deck: string[];              // 牌库
  discardPile: string[];       // 弃牌堆
  multiplier: number;          // 游戏倍率
  turn: number;                // 回合数
  currentPlayer: number;       // 当前玩家索引
  turnWinner: number | null;   // 本回合获胜者ID
  phase: string;               // 阶段: roll/draw/action
  canActThisTurn: boolean;     // 本回合是否可使用手牌
  isDueling: boolean;          // 是否正在决斗
  duelState: DuelState | null;  // 决斗状态
}

interface Player {
  id: number;
  name: string;
  health: number;              // 当前血量
  maxHealth: number;            // 血量上限 (默认10)
  dyingTurns: number;          // 濒死状态回合数
  dying: boolean;              // 是否处于濒死状态
  dead: boolean;               // 是否已死亡
  hand: string[];              // 手牌列表
  character: string | null;    // 装备的角色牌ID
}
```

### 2.3 血量变化规则

```
血量设置流程：
  1. 设置新血量
  2. 检查炸血（血量≥11 → 血量=0）
  3. 限制血量范围（0-maxHealth）
  4. 播放飘字动画
  5. 记录变化日志
  6. 处理濒死状态（血量=0时触发）
     - 方卷角色：直接死亡
     - 普通角色：进入濒死计数
  7. 检查死亡（濒死≥2回合）
  8. 更新UI
```

---

## 3. 卡牌系统

### 3.1 卡牌列表

| 卡牌ID | 名称 | 类型 | 目标 | 效果描述 |
|--------|------|------|------|----------|
| quantum | 量子力学 | spell | 指定玩家 | 50%概率随机目标血量变为0-10，50%概率自己血量变为0-10 |
| hall | 霍尔元件 | spell | 指定玩家 | 指定玩家，计算双方血量和X，50%概率自己得5/对手得X-5，50%概率反之 |
| combine | 自由组合 | spell | 全场 | 记录全场血量，随机打乱重新分配 |
| duel | 决斗 | spell | 指定玩家 | 发起石头剪刀布对决，首局胜则胜，首局负则改为三局两胜 |
| rage | 发怒 | spell | 无 | 游戏倍率×2，可无限累加 |
| kill | 杀 | spell | 指定玩家 | 扣除指定玩家1点血，目标可用【闪】闪避 |
| dodge | 闪 | response | 无 | 被动卡牌，被【杀】时可闪避 |
| peach_garden | 桃园结义 | spell | 全场 | 在场所有玩家血量+1 |
| barbarian | 南蛮入侵 | spell | 全场 | 在场所有玩家血量-1 |
| peach | 桃 | spell | 自己 | 自己血量+1 |

### 3.2 角色牌列表

| 角色ID | 名称 | 图标 | 效果 | 血量上限变化 |
|--------|------|------|------|--------------|
| fang_juan | 方卷 | 🎭 | 装备后血量上限变7，濒死时直接死亡 | 10 → 7 |

### 3.3 卡牌数据结构

```typescript
interface Card {
  id: string;                   // 唯一标识
  name: string;                 // 卡牌名称
  type: CardType;              // 卡牌类型
  target: TargetType;          // 目标类型
}

type CardType = 'spell' | 'response' | 'character';
type TargetType = 'none' | 'single' | 'self' | 'all';
```

### 3.4 角色牌数据结构

```typescript
interface CharacterDefinition {
  id: string;                   // 唯一标识
  name: string;                 // 角色名称
  icon: string;                 // 图标emoji
  desc: string;                 // 效果描述
  effects: {
    onEquip(player: Player): void;      // 装备时触发
    onUnequip(player: Player): void;    // 卸下时触发
    modifyDying(player: Player): string; // 濒死修改: 'normal' | 'instant_death'
  }
}
```

---

## 4. 效果系统

### 4.1 效果执行器

```javascript
const Effects = {
  // 基础方法
  randomHealth() { return Math.floor(Math.random() * 11); },
  shuffleArray(array) { /* Fisher-Yates洗牌 */ },
  
  // 量子力学
  quantum(targetId, userId) { /* 随机血量 */ },
  
  // 霍尔元件
  hall(targetId, userId) { /* 血量重分配 */ },
  
  // 自由组合
  combine() { /* 全场血量打乱 */ },
  
  // 决斗
  duel(targetId, userId) { /* 发起决斗 */ },
  processRPS(choice) { /* 石头剪刀布处理 */ },
  endDuel(winnerId, loserId, initiatorWon) { /* 结束决斗 */ },
  
  // 发怒
  rage() { /* 倍率翻倍 */ },
  
  // 杀
  kill(targetId, userId) { /* 扣血，检查闪避 */ },
  
  // 桃园结义
  peachGarden() { /* 全场+1血 */ },
  
  // 南蛮入侵
  barbarian() { /* 全场-1血 */ },
  
  // 桃
  peach(userId) { /* 自己+1血 */ }
};
```

### 4.2 效果类型分类

| 效果分类 | 示例卡牌 | 实现方式 |
|---------|---------|---------|
| 随机血量类 | 量子力学 | Math.random() 概率分支 |
| 重新分配类 | 霍尔元件 | 数学计算 + 随机分配 |
| 全局随机类 | 自由组合 | Fisher-Yates洗牌 |
| 倍率控制类 | 发怒 | 状态修改 |
| 交互对抗类 | 决斗 | 外部输入 + AI随机 |
| 伤害类 | 杀、南蛮入侵 | 直接扣减血量 |
| 治疗类 | 桃、桃园结义 | 直接增加血量 |
| 闪避类 | 闪 | 被动响应 |
| 角色牌类 | 方卷 | 装备效果 + 修改规则 |

---

## 5. 交互系统

### 5.1 交互类型

| 交互类型 | 适用卡牌 | 操作方式 |
|---------|---------|---------|
| 拖拽指定目标 | 量子力学、霍尔元件、决斗、杀 | 按住卡牌拖出箭头指向目标 |
| 拖拽装备角色 | 角色牌（方卷） | 按住卡牌拖出箭头指向目标 |
| 点击使用（无目标） | 自由组合、发怒、桃园结义、南蛮入侵 | 直接点击 |
| 点击使用（仅自己） | 桃 | 直接点击 |

### 5.2 箭头拖拽系统

```
拖拽流程：
  1. 按住卡牌 (mousedown)
  2. 显示SVG箭头层
  3. 绘制从卡牌中心到鼠标位置的虚线箭头
  4. 检测鼠标下的玩家区域
  5. 箭头颜色变化:
     - 普通状态: 绿色虚线
     - 锁定目标: 红色虚线
  6. 松手 (mouseup) 触发效果
```

### 5.3 弹窗系统

| 弹窗 | 触发条件 | 功能 |
|------|---------|------|
| 骰子弹窗 | 每回合开始 | 掷骰决定回合归属 |
| 闪避确认 | 被【杀】且手中有【闪】 | 询问是否使用【闪】 |
| 石头剪刀布 | 决斗时 | 交互式对决 |
| 游戏结束 | 有玩家死亡 | 显示获胜者 |

---

## 6. 动画/特效系统

### 6.1 特效列表

| 特效 | 触发条件 | 实现方式 |
|------|---------|---------|
| 抽牌动画 | 抽牌阶段 | 卡牌从上方飞入，缩放+透明度过渡 |
| 伤害飘字 | 血量减少 | 红色数字向上飘出，淡出 |
| 治疗飘字 | 血量增加 | 绿色数字向上飘出，淡出 |
| 血量跳动 | 血量变化 | 数字放大缩小动画 |
| 濒死闪烁 | 进入濒死 | 玩家区域红色闪烁3次 |
| 角色牌装备 | 装备角色 | 缩放+淡入动画 |
| 目标高亮 | 拖拽指向目标 | 边框发光效果 |

### 6.2 SVG箭头特效

```css
.arrow-line {
  stroke: #44ff44;          /* 默认绿色 */
  stroke-dasharray: 8, 6;   /* 虚线样式 */
  filter: drop-shadow(...); /* 发光效果 */
}

.arrow-line.hit {
  stroke: #ff3333;          /* 锁定目标变红色 */
  stroke-dasharray: 12, 4;  /* 虚线更密集 */
}
```

---

## 7. 状态管理系统

### 7.1 状态管理核心

```javascript
const Game = {
  state: { /* GameState */ },
  
  // 卡牌堆管理
  initDeck() { /* 初始化牌库+角色牌 */ },
  drawCard(playerId, count, callback) { /* 异步抽牌+动画 */ },
  discardCard(playerId, cardIndex) { /* 弃牌到弃牌堆 */ },
  
  // 血量系统
  setHealth(playerId, newHealth, source) { /* 血量变化+濒死处理 */ },
  
  // 角色牌系统
  equipCharacter(playerId, characterId) { /* 装备角色牌 */ },
  
  // 回合系统
  startTurnRoll() { /* 骰子阶段 */ },
  startDrawPhase() { /* 抽牌阶段 */ },
  endTurn() { /* 结束回合+濒死检查 */ },
  
  // 死亡检查
  checkDyingAtTurnEnd() { /* 回合结束濒死计数 */ },
  checkDeath() { /* 检查游戏是否结束 */ }
};
```

### 7.2 濒死与死亡处理

```
普通角色:
  血量=0 → 进入濒死(dying=true, dyingTurns=0)
  回合结束 → dyingTurns++
  dyingTurns >= 2 → 死亡(dead=true)
  血量>0 → 脱离濒死

方卷角色:
  血量=0 → 直接死亡(dead=true)，跳过濒死判定
```

---

## 8. UI系统

### 8.1 页面结构

```
game-container
├── game-info (倍率/回合/牌库/弃牌)
├── main-layout
│   ├── game-area
│   │   ├── players
│   │   │   ├── player1
│   │   │   │   ├── 血条
│   │   │   │   ├── 状态
│   │   │   │   ├── 角色牌槽位
│   │   │   │   └── 手牌区
│   │   │   └── player2 (同上)
│   │   └── 游戏日志
│   └── card-editor (卡牌编辑器)
├── SVG箭头层 (drag-arrow-layer)
└── 弹窗层 (modals)
    ├── 骰子弹窗
    ├── 闪避确认弹窗
    ├── 石头剪刀布弹窗
    └── 游戏结束弹窗
```

### 8.2 卡牌编辑器

| 功能 | 描述 |
|------|------|
| 卡牌选择 | 下拉菜单选择9种卡牌+1种角色牌 |
| 效果说明 | 显示卡牌类型、目标、效果描述 |
| 参数配置 | 可调整数值参数（血量范围、倍率等） |
| 应用配置 | 即时修改卡牌效果 |
| 效果预览 | 编辑器日志显示操作记录 |

---

## 9. 扩展接口设计

### 9.1 新增卡牌模板

```javascript
// 1. 添加到 CARD_DECK 常量
const CARD_DECK = [..., 'new_card'];

// 2. 添加名称映射
getCardName(card) {
  const names = { ..., new_card: '新卡牌' };
  return names[card] || card;
}

// 3. 添加效果
Effects.newCard(targetId, userId) {
  // 效果实现
}

// 4. 添加交互分类
const needsTarget = ['...', 'new_card']; // 或 selfOnly / noTarget

// 5. 添加CSS图标
.hand-card[data-card="new_card"]::before { content: '🆕'; color: #xxx; }
```

### 9.2 新增角色牌模板

```javascript
// 1. 添加到 CHARACTER_DECK 常量
const CHARACTER_DECK = [..., 'new_char'];

// 2. 添加到 CharacterDefinitions
const CharacterDefinitions = {
  ...,
  new_char: {
    id: 'new_char',
    name: '新角色',
    icon: '🎭',
    desc: '角色效果描述',
    effects: {
      onEquip(player) { /* 装备时效果 */ },
      onUnequip(player) { /* 卸下时效果 */ },
      modifyDying(player) { /* 濒死修改 */ }
    }
  }
};

// 3. 在 setHealth 中添加濒死逻辑检查
if (player.character === 'new_char') { /* 特殊处理 */ }

// 4. 添加CSS图标
.hand-card[data-card="new_char"]::before { content: '🎭'; }
```

---

## 10. 文件结构

```
card-game-demo/
├── index.html          # 页面入口
├── style.css           # 样式+动画+特效
├── game.js            # 游戏核心逻辑+状态管理
├── effects.js         # 卡牌效果系统+角色牌定义
├── ui.js              # UI交互+拖拽箭头+动画
└── package.json       # Electron打包配置
```

---

## 11. 开发路径

### 第一阶段：核心引擎 ✅ 已完成
1. 基础游戏状态管理 ✅
2. 卡牌打出/效果系统 ✅
3. 回合阶段管理 ✅
4. 炸血/濒死/死亡机制 ✅

### 第二阶段：效果系统 ✅ 已完成
1. 5个核心卡牌效果 ✅
2. 决斗交互系统 ✅
3. 倍率系统 ✅

### 第三阶段：扩展功能 ✅ 已完成
1. 新增4种卡牌+1种闪避 ✅
2. 角色牌系统（方卷）✅
3. SVG箭头拖拽交互 ✅
4. 伤害/治疗飘字特效 ✅
5. 抽牌动画 ✅
6. 卡牌编辑器 ✅
7. 弃牌堆+牌库管理 ✅

### 第四阶段：待完善
1. 更多角色牌设计
2. 卡牌组合技/连锁效果
3. AI对手
4. 网络对战
5. 音效系统

---

## 12. 运行方式

双击 `index.html` 用浏览器打开

---

## 13. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| 1.0 | 2026-04-21 | 初始版本，包含5个核心卡牌效果和基础规则 |
| 1.1 | 2026-04-29 | 新增4种卡牌+1种闪避，角色牌系统，SVG拖拽交互，特效动画，卡牌编辑器 |

---

*文档版本: 1.1*
*创建日期: 2026-04-21*
*更新日期: 2026-04-29*
